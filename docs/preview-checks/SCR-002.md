# SCR-002 `/about` — Preview Check (W07)

**Wave:** W07 (`TASKS/WAVE_PLAN.md` — SCR-002 대표 소개 Component와 Page Owner 2/2)
**작성일:** 2026-09-18
**상태:** 구현 완료, **Browser Checkpoint 확인 대기**(`TASKS/WAVE_STATE.json` `W07.status = "waiting_for_preview"`)

## 완료 Task (4/4)

| Task | 커밋 | 파일 |
|---|---|---|
| `C-SCR002-GALLERY` | `4374ad7` | `src/components/about/gallery.tsx` |
| `C-SCR002-MEMORABLE-CTA` | `65a55de` | `src/components/about/memorable-cta.tsx` |
| `C-SCR002-CONTACT-LINKS` | `425d984` | `src/components/about/contact-links.tsx` |
| `PAGE-SCR002` | `0242b03` | `src/app/about/page.tsx` |

**함께 커밋된 리비전(W07 소속 Task는 아님):**
- `next.config.ts`(`7384f46`) — `C-SCR002-GALLERY`의 `next/image`가 Wikimedia Commons 도메인을 최적화할 수 있도록 `images.remotePatterns` 추가.

## 대표 소개 요구사항 대조 (이번 요청 체크리스트)

| 요구사항 | 반영 위치 | 확인 |
|---|---|---|
| 대표명 free_traveler | `src/data/about.ts` `name`(W06에서 이미 수정) | ✅ |
| 대표 사진 + 한 문장 소개 | `ProfileHero`(`gallery[0]` 실제 사진 + `tagline`) | ✅ |
| 여행 50회 이상·30개국 이상 지표 | `ProfileHero`(`totalTrips`/`totalCountries`, 홈과 동일 필드 참조) | ✅ |
| 소개·여행 철학 2~4문단 | `IntroPhilosophy`(3개 섹션, 각 2~3문단) | ✅ |
| Timeline 6개 이상 | `Timeline`(7개 항목) | ✅ |
| 권역별 방문 국가 30개 | `CountryChips`(6개 권역, 총 30개국) | ✅ |
| 서로 다른 장소의 Gallery 사진 8개 이상 | `Gallery`(8장, 전부 실제 Wikimedia Commons 사진으로 이번 Wave에서 교체) | ✅ |
| 기억에 남는 여행지·경험 4개 | `MemorableCta`(4개 카드, 미해결 id 자동 대체) | ✅ |
| `/travel-tools`와 `/mates` CTA | `MemorableCta` 하단 Banner 버튼 2개 | ✅ |
| 이미지 로딩·실패 상태와 alt | `Gallery`는 `next/image`(반응형+lazy), 전 사진 alt는 실제 장소/구도 설명. 별도 에러 폴백 UI는 없음(Wikimedia URL을 이미 사전 검증해 존재 확인함) | ⚠️ 부분 — 아래 "남은 제약" 참고 |
| 모바일 반응형 | `Gallery` Desktop 3열/Mobile 2열, `ProfileHero`/`IntroPhilosophy` Desktop 좌우분할→Mobile 세로 스택, `CountryChips` Desktop 가로→Mobile 세로 | ✅ |
| 각 영역 제목+설명, 과도한 빈 여백 없음 | 전 Section에 `<h2>`+안내문 존재 | ✅ |
| 제공하지 않은 실명·개인 이력 금지 | `about.ts`(W06 수정) 유지, 이번에 추가한 Gallery 캡션도 실제 Commons 작가명만 표기 | ✅ |

## 검사 결과

- `npm run lint` — **PASS**
- `npm run typecheck` — **PASS**
- `npm run build` — **PASS** (`/about` 정적 프리렌더 확인)
- `npx playwright test tests/e2e/public-smoke.spec.ts -g "E2E-001|E2E-002"` — **2/2 PASS** (E2E-002는 `/about` 부재로 이전엔 항상 실패했었음 — 이번에 처음 통과)
- `npm run test:unit` — 대상 Unit Test 없음(SCR-002 계열은 Unit Test Task가 정의돼 있지 않음, 정상)
- Gallery 이미지 8개 URL 전수 HTTP 200 확인(이번 Wave 착수 전 별도 스크립트로 검증, W06 턴 참고)

## 남은 제약 / 수동 확인 필요

- **Gallery 이미지 에러 폴백 없음**: 홈의 `destination-grids.tsx` 카드처럼 `onError`로 깨진 이미지를 숨기는 처리가 `Gallery`에는 없습니다(next/image 사용 + 최적화 파이프라인 특성상 동일한 패턴 적용이 간단하지 않아 이번 Wave에서는 생략). 현재는 8개 URL이 전부 살아있음을 확인했지만, Wikimedia 원본이 나중에 삭제/이동되면 깨진 이미지가 그대로 노출될 수 있습니다.
- `MANUAL-CONTENT-REVIEW`, `MANUAL-PERF-LIGHTHOUSE`, `MANUAL-RESPONSIVE-VISUAL`, `MANUAL-EXTERNAL-LINKS`(각 Component의 Verify 열) — 자동 검사가 아니므로 사람이 별도로 수행해야 합니다.
- 실제 Supabase 인증이 필요한 흐름은 이 화면에 없어 DB 연결과 무관합니다.

## 다음 단계

Browser Checkpoint 확인 요청 — 아래 확인 후 알려주시면 `W07.status`를 `"done"`으로 마감합니다. **W08은 자동 시작하지 않았습니다.**
