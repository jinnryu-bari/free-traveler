# W15 — Vercel Preview와 Release 확인 — 진행 상태 (실습용)

**작성일:** 2026-09-18 (배포 시도) / 2026-09-19 (실습 배포 완료 확인)
**상태:** `W15.status = "blocked_input"` — **실습용 배포는 완료됐다**(URL 확보, 사이트 접속·기존 계정 로그인 사용자 확인). 단, `RELEASE-VERCEL-DEPLOY-CHECK`/`RELEASE-SUPABASE-CHECK`의 정식 검사 항목은 확인하지 않아 두 Task 모두 `done`으로 표시하지 않는다 — **전체 기능 통과로 기록하지 않는다.**

## 완료 — 실습 배포

- **배포 URL**: https://free-traveler-six.vercel.app (사용자가 Vercel에서 직접 연결·배포 완료)
- **GitHub push**: `origin/main` = `a5c8137`(2026-09-18 마감 기록 커밋까지 반영됨)
- **사용자 확인(2026-09-19)**: 배포 사이트 접속됨, 기존 계정으로 로그인 성공

이 두 가지만으로 "실습용 배포가 동작한다"는 목표는 달성됐다. `TASKS/WAVE_STATE.json`에 `W15.practice_deploy` 객체로 이 사실과 범위를 별도로 기록했다(Task 정식 완료 여부와 구분하기 위함).

## 완료로 표시하지 않는 것 — 정식 Release Task AC와의 차이

`RELEASE-VERCEL-DEPLOY-CHECK`/`RELEASE-SUPABASE-CHECK`의 실제 Functional AC는 이번에 확인한 범위보다 넓다:

| Task | 정식 AC | 이번에 확인한 것 |
|---|---|---|
| `RELEASE-VERCEL-DEPLOY-CHECK` | TLS 1.2+ 적용, 비밀키가 클라이언트 번들에 미포함(빌드 산출물 검사), 요금 티어 확인 | 사이트가 열린다는 것만 |
| `RELEASE-SUPABASE-CHECK` | 6개 테이블 전부 RLS 활성 확인, CSRF/SameSite 쿠키 기본 설정 확인, 개발/운영 키 분리 확인 | 로그인이 된다는 것(Supabase Auth 연결 자체는 동작함을 간접 확인)만 |

두 Task 모두 `blocked_input` 상태를 유지한다 — 정식 검사는 미실행이며, 필요하면 다음에 이어서 진행한다.

## 유지되는 것 — 기존 미검증·보류 항목 (변경 없음)

- 다른 계정의 참가 신청→승인, 신고·차단
- 관리자 기능(신고 상태 변경, 외부 URL 저장)
- 일반 사용자의 `profiles.role` 자가 승격 차단 실제 동작
- W14 잔여 항목(`MANUAL-A11Y-KEYBOARD`/`MANUAL-RESPONSIVE-VISUAL`/`MANUAL-EXTERNAL-LINKS`의 나머지 범위, `MANUAL-PERF-LIGHTHOUSE`) — 사용자 승인 보류

**이 배포는 수업 실습용 확인이며, 정식 프로덕션 릴리스 검증(성능·보안 전수 점검 등)을 대체하지 않는다.**

## 다음 한 단계

필요하면 위 "정식 Release Task AC" 표의 나머지 항목(TLS/비밀키 노출/RLS 활성/쿠키 설정)과 위에 나열된 기존 미검증 항목을 이어서 확인한다. 지금은 여기서 마감한다.
