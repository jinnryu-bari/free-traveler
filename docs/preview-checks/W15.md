# W15 — Vercel Preview와 Release 확인 — 진행 상태 (실습용)

**작성일:** 2026-09-18 (15분 제한 내 진행)
**상태:** `W15.status = "blocked_input"` — GitHub push까지 완료, Vercel 배포 확인은 사용자 입력 필요.

## 완료

- **GitHub push**: `origin/main`이 `8f90f78`(최초 스캐폴드) → `1881ec1`(오늘까지 전체 작업)로 갱신됨. 저장소: `https://github.com/jinnryu-bari/free-traveler`

## 완료하지 못한 것 — 원인

이 세션에는 **Vercel 로그인 세션도, CLI 링크(`.vercel/project.json`)도 없다.** `npx vercel whoami`를 시도했으나 인터랙티브 로그인 대기로 타임아웃됐다 — 브라우저 OAuth 로그인은 제가 대신 할 수 없다. 따라서 실제 배포 트리거·배포 URL 확인·`/`·`/account` 기본 확인까지 진행하지 못했다.

## 사용자가 확인/진행해야 할 화면 (짧게)

1. **https://vercel.com/dashboard** 로그인 후 이 GitHub 저장소(`jinnryu-bari/free-traveler`)가 이미 연결돼 있는지 확인
   - **연결돼 있다면**: 방금 push로 새 Production 배포가 이미 자동으로 시작됐을 것이다 — 해당 프로젝트의 "Deployments" 탭에서 가장 최근 배포와 URL을 확인
   - **연결돼 있지 않다면**: "Add New… → Project → Import Git Repository"에서 이 저장소를 선택하고, **Environment Variables**에 `.env.local`에 있는 두 값을 그대로 입력: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` → Deploy
2. 배포가 끝나면 그 URL을 알려주시면, 제가(또는 사용자가 직접) 홈(`/`)과 `/account`가 여는지만 기본 확인하겠습니다.

## 유지되는 것 — 기존 미검증 항목 (변경 없음)

이번 작업은 push/배포 절차에만 집중했다. 아래는 계속 미검증 상태로 남는다(`docs/preview-checks/W13.md`/`W14.md` 참고):
- 다른 계정의 참가 신청→승인, 신고·차단
- 관리자 기능(신고 상태 변경, 외부 URL 저장)
- 일반 사용자의 `profiles.role` 자가 승격 차단 실제 동작
- W14 잔여 항목(`MANUAL-A11Y-KEYBOARD`/`MANUAL-RESPONSIVE-VISUAL`/`MANUAL-EXTERNAL-LINKS`의 나머지 범위, `MANUAL-PERF-LIGHTHOUSE`) — **사용자 승인 보류로 기록, 전체 통과로 표시하지 않는다.**

**이 배포는 수업 실습용 확인이며, 정식 프로덕션 릴리스 검증(성능·보안 전수 점검 등)을 대체하지 않는다.**

## 다음 한 단계

사용자가 Vercel 대시보드에서 위 1번을 확인하고 배포 URL을 알려주면, `/`·`/account` 기본 확인을 이어서 진행한다.
